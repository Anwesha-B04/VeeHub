Place the provided background image (car scene) here as 'hero-cars.jpg'.

Path: /public/assets/hero-cars.jpg

This file is referenced by the landing page styles (`.hero` background-image).
If you already have the attached image locally, copy it:

cp /path/to/attached-image.jpg ./public/assets/hero-cars.jpg

Then refresh the dev server or hard-reload the page to see the new background.